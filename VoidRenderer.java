/**
 * Implements the renderer interface, it used when the user
 * choose not to renderer the game while running
 */
public class VoidRenderer implements Renderer{
    @Override
    public void renderBoard(Board board) {

    }
}
